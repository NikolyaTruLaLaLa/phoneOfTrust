
<div class="<%= mes.sender == "admin" ? 'my_message' : 'not_my_message' %>">
    <strong><%= mes.sender %></strong>
    <%= mes.content %>
    <small><%= mes.created_at.strftime("%H:%M")%></small>
</div>
