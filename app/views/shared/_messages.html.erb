<div id="messages">
    <!-- работает без этого order(:created_at). WHYYY?-->
     <% chat.messages.each do |mes| %>
        <%= render "shared/message_#{current_sender}", mes: mes%>
    <% end %> 
</div>